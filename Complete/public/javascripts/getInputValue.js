var inputVal;
    function getInputValue(){
            // Selecting the input element and get its value
             inputVal = document.getElementById("SPsearch");
            
   //inputVal.addEventListener("keyup")
    // Displaying the , e => { 
            alert(inputVal);
        }
